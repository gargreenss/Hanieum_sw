"""음식 판별 모듈 — run_test.py와 server.py가 공유 (쌍별 구분 기준 v3)"""
import anthropic, base64, io, re
from PIL import Image

MODEL = "claude-haiku-4-5"
MAX_SIZE = 1024

FAST_MODE = True
MAX_TOKENS = 20 if FAST_MODE else 350

FOOD_LIST = """김치(불규칙하게 접히거나 겹친 채소 조각 — 울퉁불퉁한 표면, 붉은색·주황색·분홍색 계열)
계란말이(계란 특유의 노란색·연노란색과 매끈한 계란 조직, 말린 층 또는 반복되는 층 구조가 있는 잘린 조각)
된장국(연속된 국물 표면 — 액체 광택 또는 반사, 건더기가 국물에 잠겨 있음)
흰밥(여러 쌀알이 불규칙하게 뭉친 밝은 덩어리 — 울퉁불퉁한 알갱이 표면)
김(검은색~짙은 녹색의 매우 얇고 평평하며 마른 판/시트)"""  # 30종 확정되면 교체

VALID_FOODS = {line.split("(")[0].strip() for line in FOOD_LIST.splitlines()}
VALID_FOODS |= {"없음", "알 수 없는 음식"}

_COMMON = f"""당신은 시각장애인의 식사를 돕는 음식 판별 시스템입니다.

인식 가능한 음식:
{FOOD_LIST}

입력 이미지는 식기 끝 주변을 잘라낸 사진입니다.
젓가락, 숟가락, 포크가 가리키거나 집고 있는 음식을 판별하세요.

식기가 보이지 않더라도 정상입니다.
식기가 보이지 않는다는 이유로 "없음"이라고 판단하지 마세요.
사진 중앙에 보이는 음식이 판별 대상입니다.

촬영 환경 때문에 강한 조명, 과노출, 흐림이 있을 수 있습니다.
따라서 색뿐 아니라 형태, 질감, 액체 여부, 층 구조를 함께 사용하세요.

★ 매우 중요한 규칙

사진 중앙에 음식처럼 보이는 고체 또는 액체가 조금이라도 있다면
"없음"을 답하지 마세요.

"없음"은 다음과 같은 경우에만 허용됩니다.
- 중앙이 빈 식탁
- 빈 그릇 바닥만 보임
- 식기만 있고 음식이 전혀 없음
- 음식으로 볼 만한 고체나 액체가 중앙에 전혀 없음

사진이 흐리거나 일부만 보인다는 이유로 "없음"을 선택하면 안 됩니다.

음식이 보이면 먼저 반드시 아래 5종을 비교하세요.

김치
계란말이
된장국
흰밥
김

5종 중 가장 가까운 하나를 선택하는 것이 기본입니다.

"알 수 없는 음식"은 음식은 확실히 보이지만
위 5종 어느 것과도 형태가 명백하게 맞지 않을 때만 사용하세요.

★ 계란말이 오판 방지 — 매우 중요

계란말이는 다음 특징 중 계란을 나타내는 특징이 명확히 있어야 합니다.

- 노란색 또는 연노란색의 계란 색
- 매끈하고 균질한 계란 조직
- 말려 있는 층이나 반복되는 계란 층
- 잘린 계란 단면

다음 특징만으로는 절대 계란말이라고 판단하지 마세요.

- 하얗거나 밝음
- 네모처럼 보임
- 각져 있음
- 덩어리 형태
- 매끈해 보임
- 작은 접시에 있음

과노출 때문에 색이 희게 날아갔더라도
계란 특유의 층 또는 조직이 보이지 않는다면
단순한 모양만 보고 계란말이를 선택하지 마세요.

계란말이를 선택하기 전에 반드시 아래 네 가지를 먼저 확인하세요.

1. 쌀알처럼 불규칙한 작은 알갱이들이 보이면 → 흰밥
2. 접히고 울퉁불퉁한 채소 조각이면 → 김치
3. 연속된 액체 표면이 보이면 → 된장국
4. 매우 얇은 검정/짙은 녹색 판이면 → 김

위 네 가지 특징이 없고,
계란 특유의 색·층·조직이 있을 때 계란말이를 선택하세요.

★ 음식별 판별 기준

[김치]
- 크기와 형태가 일정하지 않은 조각
- 접히고 겹친 채소 잎
- 울퉁불퉁한 표면
- 붉은색, 분홍색, 주황색 계열
- 양념이 묻거나 약간의 국물이 있을 수 있음

색이 희게 날아가도
불규칙하고 접힌 채소 구조가 보이면 김치를 우선하세요.

[흰밥]
- 많은 작은 알갱이가 모여 있음
- 불규칙하게 뭉친 덩어리
- 울퉁불퉁한 표면
- 흰색 또는 밝은색

과노출로 쌀알 경계가 약해져도
전체 형태가 일정한 블록이 아니라
불규칙하게 뭉쳐 있으면 흰밥을 우선하세요.

[된장국]
- 연속적으로 이어진 액체 표면
- 액체 반사 또는 광택
- 건더기가 액체 속에 잠겨 있음
- 그릇 안쪽에 국물이 퍼져 있음

두부 같은 밝은 건더기가 중앙에 있어도
그 주변에 국물이 명확하면 된장국입니다.

[김]
- 매우 얇음
- 평평한 판 또는 시트
- 검정색 또는 짙은 녹색
- 건조한 고체

검은 그림자나 어두운 국물은 김이 아닙니다.

[계란말이]
- 노란색 또는 연노란색 계란 조직
- 비교적 일정한 잘린 조각
- 계란이 말린 층 또는 단면
- 매끈하고 균질한 계란 표면

계란의 색, 층, 조직 중 하나도 확실하지 않다면
단순히 네모나 밝다는 이유로 계란말이를 선택하지 마세요.

★ 헷갈릴 때의 구분

[김치 vs 계란말이]
- 불규칙하고 접힌 채소 조각 = 김치
- 일정한 두께 + 계란 특유의 층/조직 = 계란말이
- 밝기나 네모난 모양만으로 계란말이를 고르지 마세요.

[흰밥 vs 계란말이]
- 불규칙하게 뭉친 알갱이 표면 = 흰밥
- 규칙적인 절단면 + 계란 조직/층 = 계란말이
- 과노출된 밝은 덩어리를 자동으로 계란말이라고 판단하지 마세요.

[김치 vs 된장국]
- 개별 채소 조각이 중심 = 김치
- 연속된 액체 표면이 중심 = 된장국

[김 vs 된장국]
- 얇고 평평한 고체 시트 = 김
- 액체 표면 = 된장국

★ 최종 판단 순서

1. 사진 중앙에 음식이 있는지 확인합니다.
2. 음식이 있으면 "없음"은 후보에서 제외합니다.
3. 액체면 된장국을 먼저 확인합니다.
4. 얇은 검은 시트면 김을 확인합니다.
5. 알갱이가 뭉쳐 있으면 흰밥을 확인합니다.
6. 불규칙하고 접힌 채소 조각이면 김치를 확인합니다.
7. 위 특징이 아니면서 계란 특유의 색·층·조직이 있으면 계란말이를 확인합니다.
8. 음식은 있지만 위 5종 어느 것에도 명백하게 맞지 않을 때만 "알 수 없는 음식"입니다.

중요:
다섯 음식의 발생 확률은 동일하다고 가정하세요.
이전에 어떤 음식을 답했는지는 현재 사진 판단에 영향을 주면 안 됩니다.
매 사진을 독립적으로 판단하세요.

음식 재료 이름을 답하지 마세요.
예를 들어 된장국 속 두부를 가리키더라도 답은 "된장국"입니다.

가장자리의 다른 음식보다 사진 중앙의 음식을 우선하세요."""

if FAST_MODE:
    PROMPT = _COMMON + """

반드시 아래 7개 중 정확히 하나만 답하세요.

김치
계란말이
된장국
흰밥
김
없음
알 수 없는 음식

사진 중앙에 음식이 보이면 "없음"을 답하지 마세요.
계란 특유의 색·층·조직 없이 단순히 밝거나 네모난 것만으로 계란말이를 답하지 마세요.

다른 말 없이 답 하나만.
설명, 이유, 문장, 서식 금지."""
else:
    PROMPT = _COMMON + """

1단계: 식기 끝 또는 사진 중앙의 판별 대상 확인
2단계: 액체/시트/알갱이/채소 조각/계란 조직 순서로 특징 비교
3단계: 가장 잘 맞는 음식 선택

굵게 표시(**) 등 어떤 서식도 사용하지 마세요.
마지막 줄은 반드시 "최종답: OOO" 형식으로, 음식 이름만 쓰세요."""

client = anthropic.Anthropic()  # ANTHROPIC_API_KEY 환경변수 사용


def encode_image(img: Image.Image) -> str:
    img = img.convert("RGB")
    img.thumbnail((MAX_SIZE, MAX_SIZE))
    buf = io.BytesIO()
    img.save(buf, format="JPEG", quality=85)
    return base64.standard_b64encode(buf.getvalue()).decode()


def recognize_food(img: Image.Image) -> dict:
    msg = client.messages.create(
        model=MODEL, max_tokens=MAX_TOKENS,
        messages=[{"role": "user", "content": [
            {"type": "image", "source": {"type": "base64",
             "media_type": "image/jpeg", "data": encode_image(img)}},
            {"type": "text", "text": PROMPT},
        ]}],
    )

    full = msg.content[0].text

    m = re.search(r"최종답\s*[:：]\s*(.+)", full)
    food = m.group(1) if m else full

    food = food.replace("*", "").strip().rstrip(".")

    if food not in VALID_FOODS:
        print(f"[판별] 목록 밖 응답 차단: '{food}' → 알 수 없는 음식")
        food = "알 수 없는 음식"

    return {
        "food": food,
        "raw": full,
        "input_tokens": msg.usage.input_tokens,
        "output_tokens": msg.usage.output_tokens,
    }
