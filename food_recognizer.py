"""음식 판별 모듈 — run_test.py와 server.py가 공유 (쌍별 구분 기준 v3)"""
import anthropic, base64, io, re
from PIL import Image

MODEL = "claude-haiku-4-5"
MAX_SIZE = 1024

FAST_MODE = True
MAX_TOKENS = 20 if FAST_MODE else 350

FOOD_LIST = """김치(불규칙하게 접히거나 겹친 채소 조각 — 붉은색·분홍색·주황색 계열, 색이 바래도 조각 형태가 불규칙하면 김치 가능성)
계란말이(비교적 일정한 두께와 규칙적인 절단면 — 노란빛, 매끈한 계란 질감 또는 말린 층이 보일 수 있음)
된장국(그릇에 담긴 국물 — 연속된 액체 표면과 광택, 건더기가 국물에 잠겨 있을 수 있음)
흰밥(흰색~밝은색의 쌀밥 — 작은 알갱이가 모여 불규칙하게 뭉친 질감)
김(검은색~짙은 녹색의 얇고 평평한 마른 판/시트)"""  # 30종 확정되면 교체

VALID_FOODS = {line.split("(")[0].strip() for line in FOOD_LIST.splitlines()}
VALID_FOODS |= {"없음", "알 수 없는 음식"}

_COMMON = f"""당신은 시각장애인의 식사를 돕는 시스템입니다.

인식 가능한 음식 목록:
{FOOD_LIST}

사진에서 식기(젓가락, 숟가락, 포크)의 끝이 가리키는 음식을 찾으세요.
- 식기로 음식을 집거나 뜨고 있으면 그 음식이 답입니다.
- 이 사진은 식기 끝 주변만 잘라낸 것이라 식기가 잘 안 보일 수 있습니다.
  식기가 안 보여도 사진 중앙의 음식을 우선 판단하세요.
- 사진 중앙에 음식이 보이면 가능한 한 위 목록 중 가장 잘 맞는 음식을 선택하세요.
- "없음"은 음식이 전혀 보이지 않을 때만 답하세요.
- 음식은 보이지만 위 5종 어느 것과도 거의 맞지 않을 때만 "알 수 없는 음식"이라고 답하세요.

촬영 환경상 강한 조명 때문에 색이 바래거나 사진이 흐릴 수 있습니다.
색만으로 판단하지 말고 모양·질감·액체 여부·표면 구조를 함께 보세요.
단, 모든 특징이 완벽하게 보여야 하는 것은 아닙니다.

★ 중요한 원칙:
- 특정 음식 하나를 우선적으로 선택하지 마세요.
- 한 가지 특징만으로 음식을 결정하지 마세요.
- 밝거나 흰색이라는 이유만으로 계란말이라고 판단하지 마세요.
- 네모나 각져 보인다는 이유만으로 계란말이라고 판단하지 마세요.
- 음식이 실제로 보인다면 조금 애매하더라도 먼저 목록 5종 중 가장 가까운 것을 비교하세요.
- "알 수 없는 음식"은 5종 중 어느 것도 적절하지 않을 때 사용하세요.
- "없음"은 빈 식탁, 빈 그릇, 식기만 보이는 경우처럼 음식 자체가 없을 때만 사용하세요.

★ 헷갈리기 쉬운 구분 (반드시 이 기준으로 판단):

[김치 vs 계란말이]
- 불규칙하게 접히거나 겹친 채소 조각, 울퉁불퉁한 표면 = 김치
- 비교적 일정한 두께, 규칙적인 절단면, 매끈한 계란 질감이나 층 = 계란말이
- 단순히 밝거나 각져 보인다는 이유만으로 계란말이를 선택하지 마세요.

[김치 vs 된장국]
- 연속된 액체 표면이 넓게 보이고 건더기가 액체에 잠겨 있음 = 된장국
- 개별적인 불규칙한 채소 조각들이 중심 = 김치
- 김치에 양념이나 김칫국물이 조금 고여 있어도 조각이 중심이면 김치입니다.

[계란말이 vs 흰밥]
- 일정한 두께와 비교적 규칙적인 경계, 매끈한 절단면 또는 층 구조 = 계란말이
- 불규칙하게 뭉쳐 있고 작은 알갱이 또는 울퉁불퉁한 표면 = 흰밥
- 과노출 때문에 쌀알이 잘 안 보여도 불규칙하게 뭉친 밝은 음식이면 흰밥을 고려하세요.
- 단순히 하얗거나 밝다는 이유만으로 계란말이를 선택하지 마세요.

[김 vs 된장국/그림자]
- 얇고 평평하며 마른 고체 판/시트 = 김
- 연속된 액체 표면과 광택 = 된장국
- 단순히 검거나 어둡다는 이유만으로 김이라고 판단하지 마세요.

[김 vs 다른 음식]
- 김은 매우 얇고 평평한 판 또는 시트 형태여야 합니다.
- 검은 그림자, 어두운 그릇, 탄 부분 등은 김으로 판단하지 마세요.

주의: 음식의 재료(두부, 계란, 버터 등)가 아니라 위 목록의 음식 이름으로 답하세요.
예: 국물 속 두부를 가리키면 답은 "된장국"입니다.

중요: 사진 중앙 부근에 있는 음식이 답입니다. 가장자리에 다른 음식이
더 크고 선명하게 보여도 그것은 답이 아닙니다.

판단 우선순위:
1. 먼저 사진 중앙에 음식이 실제로 있는지 확인
2. 음식이 있다면 위 5종 중 가장 잘 맞는 것을 비교
3. 액체인지 고체인지, 규칙적인지 불규칙한지, 얇은 시트인지 뭉친 음식인지 확인
4. 색은 보조 단서로 사용
5. 정말 5종 어느 것과도 맞지 않을 때만 "알 수 없는 음식"
6. 음식 자체가 없을 때만 "없음" """

if FAST_MODE:
    PROMPT = _COMMON + """

반드시 위 목록에 있는 이름 그대로, 또는 "없음"/"알 수 없는 음식" 중 하나만 답하세요.

음식이 보인다면 가능한 한 먼저 아래 5개 중 가장 가능성이 높은 하나를 선택하세요:
김치, 계란말이, 된장국, 흰밥, 김

"없음"은 음식 자체가 전혀 없을 때만 사용하세요.
"알 수 없는 음식"은 음식은 있지만 위 5종 어느 것에도 거의 맞지 않을 때만 사용하세요.

목록에 없는 이름(예: 두부, 버터, 반찬 재료명)은 절대 답하지 마세요.
다른 말 없이 답 하나만. 설명, 서식, 문장 금지."""
else:
    PROMPT = _COMMON + """

1단계: 식기 끝 또는 사진 중앙의 음식이 어느 위치에 있는지 한 문장으로 설명
2단계: 액체 여부, 형태, 질감, 색을 비교하여 위 5종 중 가장 가까운 음식을 선택
3단계: 5종 어느 것과도 맞지 않는 경우에만 "알 수 없는 음식"을 선택

굵게 표시(**) 등 어떤 서식도 사용하지 마세요.
마지막 줄은 반드시 "최종답: OOO" 형식으로, 음식 이름만 쓰세요."""

client = anthropic.Anthropic()  # ANTHROPIC_API_KEY 환경변수 사용


def encode_image(img: Image.Image) -> str:
    img = img.convert("RGB")
    img.thumbnail((MAX_SIZE, MAX_SIZE))
    buf = io.BytesIO()
    img.save(buf, format="JPEG", quality=85)
    return base64.standard_b64encode(buf.getvalue()).decode()


def recognize_food(img: Image.Image) -> dict:
    msg = client.messages.create(
        model=MODEL, max_tokens=MAX_TOKENS,
        messages=[{"role": "user", "content": [
            {"type": "image", "source": {"type": "base64",
             "media_type": "image/jpeg", "data": encode_image(img)}},
            {"type": "text", "text": PROMPT},
        ]}],
    )
    full = msg.content[0].text
    m = re.search(r"최종답\s*[:：]\s*(.+)", full)
    food = m.group(1) if m else full
    food = food.replace("*", "").strip().rstrip(".")

    if food not in VALID_FOODS:
        print(f"[판별] 목록 밖 응답 차단: '{food}' → 알 수 없는 음식")
        food = "알 수 없는 음식"

    return {
        "food": food,
        "raw": full,
        "input_tokens": msg.usage.input_tokens,
        "output_tokens": msg.usage.output_tokens,
    }
